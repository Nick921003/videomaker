# 前端雙欄改版與版面模式選擇器 — 實作與驗證報告

**日期**：2026-08-27  
**目標分支**：  
**修改檔案**：（僅此單一檔案）  
**對應規格**：
- 
- 

---

## 1. 外殼結構設計（Shell Structure）與設計決策

本次改版將原本單欄垂直堆疊架構重構為**持久雙欄外殼（Persistent Two-Column Shell）**：

- **版面切割**：
  - 外層容器  設定 、，由頂部  與主要雙欄區  組成。
  -  使用 CSS Grid 採  比例切分為「左欄：工作區（）」與「右欄：進度側欄（）」。
  - 右欄設定最小寬度下限 300px，確保 8 個階段卡片與進度資訊不被擠壓變形。
- **畫面切換機制保留**：
  - 左欄工作區容納五個畫面區塊（、、、、），完全相容既有  機制。
  - 右欄  脫離互斥畫面切換，成為常駐可見（Persistent）組件，讓使用者在任何階段皆可即時掌握流程進度。

---

## 2. 捲動權屬（Scroll Ownership）與視窗不捲動機制

徹底解決使用者反饋之「整頁一直往下滑」痛點：

1. **整頁與 Body 零垂直捲軸**：
   -  設定 。
   -  亦設定 ，完全杜絕頁面產生全域捲軸。
2. **左欄工作區獨立擁有捲動權**：
   -  設定 。
   - 當審稿內容或文字長度超出視窗時，僅有左欄內部獨立捲動。
3. **右欄釘住全程可見**：
   - 8 個階段卡片採直排緊湊設計（單卡高度約 38px，包含總進度條整體面板高度約 380px）。
   - 在 900px 或 1080px 視窗高度下完整陳列，右欄不需要且不產生任何捲軸。
4. **審稿懸浮控制（Sticky Elements）**：
   - 審稿倒數計時橫幅  設定 。
   - 審稿操作列  設定 。
   - 即使使用者在左欄捲動至第 5 頁修改講稿，倒數計時與操作確認按鈕仍然釘在視野內，右側階段卡亦全程清晰可見。

---

## 3. 審稿排版改進（Side-by-Side Review Layout）

針對規格 2.5 節第 3 點進行排版重構：
- 原版為投影片佔滿欄寬、逐字稿垂直堆疊在下方，導致審稿一頁即吃掉一個半螢幕高度。
- 新版在  採用 CSS Grid 雙欄排版（，約 40% : 60%）：
  - 投影片標題  橫跨整行（）。
  - 投影片預覽圖  佔據左側 40% 欄位（）。
  - 各旁白段落文字框自動排列於右側 60% 欄位（）。
- **驗收標準達成**：在 1920×1080 視窗中，單頁高度大幅壓縮，第一頁之倒數計時、投影片預覽與該頁旁白輸入框可同時進入視野，無需任何捲動即可立即審稿。
- **資料屬性完整性**：完全保留  與 ，確保  讀取無誤。

---

## 4. 投影片版面模式選擇器（Layout Mode Selector）

依據規格第 4 節，於上傳區加入四選一單選按鈕組：
- **選項**：
  1. 隨機（，預設選取）
  2. 自動（）
  3. 雙欄（）
  4. 置中單欄（）
- **行為契約**：
  - 送出上傳表單時， 附帶：
    - : 所選之模式代碼。
    - : 當模式為  時每次由前端擲出新整數（）；其餘模式送 。
- **ID 零污染**：選擇器元件以  與 CSS class 組織，無任何額外 uid=1000(pjw92) gid=1000(pjw92) groups=1000(pjw92),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),100(users),1001(docker) 屬性。

---

## 5. 響應式斷點（Narrow Screen < 900px）

依據規格第 3 條第 5 項：
- 螢幕寬度小於 900px 時：
  - 外殼退回單欄流式佈局， 恢復 。
  -  設定 ，使進度面板排在工作區**最上方**，符合行動裝置優先掌握進度之體驗。
  -  切換為 4 欄網格（<=600px 時為 2 欄），節省垂直空間。
  - 審稿區  切回垂直堆疊以確保窄螢幕下之閱讀性。

---

## 6. 嚴格驗證數據與量測指標

### 6.1 ID 完整性檢查（ID Integrity）
- **改動前 ID 總數**：45
- **改動後 ID 總數**：45
- **差集**：
  - 改動後多出的 ID: （0 個）
  - 改動後遺失的 ID: （0 個）
- **完整 45 個 ID 名單**：
  , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , 。

### 6.2 Script 差異比對（Script Diff）
除了上傳表單送出時新增  與  的四行程式碼外，其餘 JavaScript 區塊與原版本完全一致：


### 6.3 色票規範遵循（Palette Verification）
檔案內出現的所有十六進位色彩碼皆完全符合十個指定暖色票，無任何未授權色碼、無  /  /  /  /  / ：
- : 
- : 
- : 
- : 
- : 
- : 
- : 
- : 
- : 
- : 

### 6.4 外部資源零相依（No External Resources）
- 檢查指令：0
- 結果：**0**（無引入任何外鏈字型、CDN 或 CSS/JS 庫）。

### 6.5 伺服器與自動化測試驗證
- 啟動  成功驗證 HTTP 200 回應與 HTML 結構。
- 執行專案單元測試套件：，共 141 項測試全部通過（）。

---

## 7. 限制與未驗證項目說明

- **全流程真實 GPU TTS / 付費 LLM 生成**：依指示在離線與無本機 GPU TTS 環境下未觸發實際即時影音合成，但已透過單元測試、HTTP 服務檢驗與靜態語法規範完整確認畫面切換與資料流相容性。
